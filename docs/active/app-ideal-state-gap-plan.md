# One Person Lab App Ideal-State Gap Plan

Owner: `one-person-lab-app`
Purpose: `app_ideal_state_gap_plan`
State: `active_plan`
Machine boundary: Human-readable active truth and gap plan. Machine-readable truth lives in `contracts/`, source, release artifacts, updater metadata, and test results.

Package composition Phase 1 is complete and Phase 2 per-family retirement is in progress. The sole
Package-composition planning and implementation authority is
[`opl-package-platform-composition-migration.md`](opl-package-platform-composition-migration.md),
whose content state is `phase_2_family_retirement_in_progress`. This gap plan may report
fresh canonical and owner-bound source-checkpoint facts, but it does not create a second
implementation plan or authorize canonical absorption, publication, installed-state mutation,
cleanup, or completion claims. Stable, Latest, Package publication and Foundry remain independent
authority lanes; zero-overlap owners continue independently.

Currentness rule: this active plan keeps App functional / structural
gaps only: App-owned contracts, active-shell sync, GUI definition stack,
Settings / Storage, release-boundary validators, shell-candidate policy, route
receipts, first-run policy, and runtime/domain no-authority guards. Release
cohorts, clean-VM proof, packaged `.app` smoke, same-cohort user-path artifacts,
real user-directory E2E, owner acceptance, and production-ready claims are
separate release / lifecycle evidence lanes. They should not be mixed into the
ideal-state or current-state gap list, and App validation or shell tests cannot
replace them.

Functional and structural lanes come first: App-owned contracts, active-shell
adapter sync, AionUI mainline GUI behavior, `opl-native-workbench` foreground-alternative
convergence, page-state matrices, route receipts, first-run policy, Settings /
Storage, release-boundary validators, and runtime/domain no-authority guards.
Release cohorts, clean-VM evidence, packaged `.app` smoke, same-cohort user-path
artifacts, real user-directory E2E, owner acceptance, and production-ready
claims are not maintained in this active plan. They remain mandatory for release
or production claims, but they must not block independent App contract/shell
cleanup, and App validation or shell tests cannot replace them.

Maintenance simplification has one owner/status ledger:
[`over-engineering-cleanup.md`](over-engineering-cleanup.md). Rows in this plan may
reference the same product surface or report its independent five-axis evidence,
but they do not own cleanup scope, ordering, or execution state and must not copy
or advance that ledger.

The target Package/Agent/Runtime migration is specified in
[`opl-package-platform-composition-migration.md`](opl-package-platform-composition-migration.md).
That plan defines functionality equivalence and implementation order; this file
records only its App product gap and independent evidence axes.

## Current State Summary

This is an owner/routing index, not a completion ledger. Its classification values
must not be read as Contract, Source, Pixel, Install, or Release status; those axes
live only in the five-axis ledger below and in `shell-conformance-matrix.md`.

| Area | Routing/classification | Current readout |
| --- | --- | --- |
| App product boundary | `product_authority` | App owns desktop packaging, release assets, updater metadata, first-run checks, screenshots, user guides, App contracts, and GUI page-state validation. |
| Purpose-first App role | `product_role` | The App is the purpose-first Codex wrapper/product truth for ordinary research, grant, presentation, book, and general work entry. Runtime truth, domain readiness, owner receipts, memory/artifact bodies, and family production readiness remain outside App ownership. |
| Framework / domain split | `owner_boundary` | OPL Framework owns runtime/provider/read-model/action execution; MAS/MAG/RCA/BookForge/OMA own domain truth, quality/export verdicts, memory body, artifact body, owner receipts, and typed blockers. |
| Active shell boundary | `external_carrier_boundary` | `shells/aionui/` is an external checkout from `gaofeng21cn/opl-aion-shell`; shell implementation history is not merged into the App repo default branch. |
| Product contracts | `machine_authority` | `contracts/app-product-profile.json`, `contracts/app-page-state-matrix.json`, `contracts/app-first-run-test-matrix.json`, and `contracts/app-release-channel.json` hold App-owned machine policy. Release evidence remains cohort-bound and artifact-backed. |
| Runtime Work Item projection | `target_core_dynamic_agent_runtime` | **Target/planned:** Runtime becomes a core dynamic task surface for installed Agent Packages. Agent owns business lifecycle, Temporal owns execution, Framework joins, App renders generic fields and typed views. Current AionUI `X0-01` WorkItem route remains a source-implemented compatibility bridge; its old optional classification is not target completion. |
| Settings Maintenance operations | `surface_owner` | Provider/Temporal/worker readiness, platform repair, managed dependencies, software update, reinstall, maintenance actions, and read-only diagnostics belong to Maintenance. They may expose typed impact, confirmation, execution, and receipts, but never appear as paper/task progress or Runtime controls. |
| Settings Agents package lifecycle | `target_native_lifecycle_surface` | **Target/planned:** Agents provides one compact UI over native-platform install/update/uninstall plus Framework installed discovery/presence/status aggregation. Lock, payload, receipt, LKG, physical materialization, version resolver, and rollback state are migration-only diagnostics, not protected UX. |
| Settings Capabilities health | `target_discovered_capability_surface` | **Target/planned:** Skill, Tool, Plugin, MCP, Agent producer, and typed view identities are discovered from installed Packages/platforms. App has no parallel allowlist or catalog; user visibility and narrow explicit product cuts affect presentation only. |
| Role-neutral Package contributions | `owner_bound_source_foundation` | The App-owned `opl-app-contributions.v1` contract, Framework directory/App-state projection, and AionUI parser/resolver foundation are implemented on owner-bound task branches. This does not yet provide a production navigation mount, a standard-view renderer, a Relay data/action bridge, Pixel evidence, installed behavior, or Release proof. |
| Settings Maintenance diagnostics | `surface_owner` | Full App state, operator drilldown, State Index/SQLite refs, raw IDs, logs, command refs, safe-action catalog, and technical receipts belong to read-only Maintenance diagnostics. Advanced is retired and redirects there. Runtime has no full-operator-drilldown exception. |
| Inspector artifact provenance | `surface_owner` | Current pointer, artifact refs/previews, lineage, retention, conformance, manifest/hash/receipt/blocker refs belong to the task/conversation Inspector. Runtime does not contain artifact drilldown and the App remains refs-only. |
| App state/display/action map | `projection_map` | `contracts/app-runtime-bridge.json#canonical_state_display_action_map` records runtime, task, and package rows from canonical App/root state to Aion display role, optional OPL Native Workbench display role, allowed App action refs, and fallback/no-overclaim policy. The Runtime row locks `producer_required=true`, `aionui_optional_route=true`, and `native_phase_one_required=false`; route-specific validation remains explicit. |
| Claude Science task-awareness landing | `current_task_x0_split` | Framework readback still owns task status and next-action semantics. Conversation current-task context and Inspector refs remain thin core consumers; the cross-project Runtime list is only the retained X0-01 route and must not become their completion dependency. |
| OpenScience accepted App items | `projection_boundary` | OpenScience accepted items are App-owned projection contracts: `structured_result_panel_projection` lands in conversation/current-task/right inspector, artifact/provenance is a refs-only card/drawer, ref-level comments are follow-up prompt/action refs without an App annotation store, and workflow/skill candidates are report-first Settings / Capabilities refs with review, needs changes, and continue in conversation actions. OPL App currently uses a Codex shell/executor; professional agents are `OPL Package(kind=agent)`, while Codex Plugin/Skill is only the current adapter/carrier projection, not Package identity or complete installed truth. |
| Hosted Workspace / Fabric / HPC / Console | `optional_owner_projected_resource_refs` | These are X0-03/X0-04. App contracts, fixtures, validators, and active AionUI source expose only optional owner-projected resource refs. Resources & Connections independently gates workspace and external groups; an absent projection creates no group, anchor, placeholder, or hosted-platform promise. |
| Agent installation exposure | `current_compatibility_surface_target_replacement` | Current contracts still define manifest/shortcut/invocation/lock surfaces and fixed starter fixtures. The target replaces them with a minimum Package/capability descriptor, one Official Profile, native installed discovery, dynamic Home, and dynamic Runtime. |
| Managed update background policy | `target_native_independent_update` | **Target/planned:** enumerate only installed Packages and call each native updater independently. Framework aggregates fresh terminal status; one failure is local. Official Profile is first-install/explicit-restore only, so absent user-removed roots are never reinstalled by background maintenance. |
| Release user-path evidence | `release_evidence_route` | Release tooling owns the complete same-cohort evidence bundle. `runtime_screenshot` is conditional and becomes required only when `runtime_route_evidence_requested`; operator/full-state captures, action receipts, VM/installed-App smoke, remote verification, and manifests remain outside Runtime and do not become App/domain/family readiness authority. |
| Codex App positioning | `product_positioning` | The App ordinary path is a Codex CLI fixed-executor experience with configurable professional-agent shortcuts. MAS/MAG/RCA/BookForge/OMA are first-party starter packages/shortcuts; AionUI multi-backend choices are implementation material, not the user-facing App model. |
| Codex subagents / AionUI Team split | `five_axis_pointer` | P3 is authoritative: Codex runtime/execution, App Server metadata intake, canonical discovery, and ordinary read-only Active/Done/detail/result/open-thread UI are source-implemented. Team stays disabled and hidden without suppressing delegated execution. Pixel, Install, and Release remain unverified. |
| Thread operations / X0-05 private coordination | `five_axis_pointer` | The active GUI contract keeps one user-triggered App Server thread-directory/action adapter and forbids a second client, private ledger, model delivery, independent page, and cross-host handoff. P1c App machine truth and Native main remove the retired private coordination requirements while preserving standard thread lifecycle and read-only Codex subagent projection. Source is implemented; later evidence axes remain unverified. |
| GUI 定义栈 | `definition_stack` | `docs/product/gui/ideal-interaction-spec.md` 定义理想 Codex App 形态交互模型，`docs/product/gui/codex-to-opl-app-delta.md` 定义 OPL 专用产品增量，`docs/product/gui/feature-inventory.md` 继续维护跨 shell 能力清单。 |
| Active shell upstream intake | `external_carrier_currentness` | `contracts/app-shell-adapter.json#shell_source.upstream_ref` records the current active shell upstream ref; shell implementation history stays in `gaofeng21cn/opl-aion-shell`. Local App validation reads `shells/aionui`, which is an ignored external shell checkout and may be a symlink to the canonical sibling checkout; if a local checkout is stale, check the shell source repo before treating active-shell validation as App contract drift. |
| GUI mainline / alternative policy | `carrier_role_policy` | AionUI is the App GUI mainline. `opl-native-workbench` is the foreground alternative candidate once `contracts/app-shell-candidates.json` selects it; it is an independent shell repo/checkout governed by `contracts/shell-adapters/opl-native-workbench.json`. Hermes Desktop / `hermes-codex` is retained as a prior-candidate reference and AGUI/CopilotKit as archived proof. The default `validate:shell-candidates` path now validates only the fixed `active / foreground / retained / archived` role registry. Native detail uses `validate:candidate:native`; Hermes and AGUI use their explicit adapter/runbook replay entries. Their detailed snapshots no longer live in the active registry or block AionUI, full, release-boundary, model-policy, or design-system gates. `validate:shell-convergence` remains a thin active-shell plus role-registry aggregate, not a second truth source. |
| OPL Native Workbench non-live product surface | `candidate_boundary` | The foreground candidate target now includes basic UI modules, artifact preview tabs, provenance drawer, starter forms, confirmation/interview cards, desktop/WebUI same-renderer parity, and source visual smoke as the current non-live acceptance scope. This closes only the App docs/product-surface definition gap; source validation, WebUI smoke, source visual artifacts, package evidence, VM, owner acceptance, active-shell adoption, and release-ready proof remain separate evidence lanes. |
| Home shell conformance | `surface_contract` | App contracts and shell tests require the Home input to hide executor/backend/provider selectors while keeping App-owned model/reasoning and user-language permission/access controls visible, plus the current starter entries. |
| Conversation shell conformance | `surface_contract` | App contracts now require ordinary Codex conversations to hide backend/provider selectors while preserving the App-owned model/reasoning and user-language permission/access controls; shell tests cover Home and conversation composers. |
| Codex CLI tool-output rendering | `source_behavior` | The active shell normalizes Codex ACP `raw_output` from `aggregated_output`, `formatted_output`, `stdout`, and `stderr` fields and preserves newline-bearing tool output in the conversation view; the shell regression test covers the newline case. |
| Professional agent invocation receipt | `current_compatibility_evidence` | Current route smoke and physical-surface display remain existing evidence paths. They are not protected target capabilities; the migration must inventory real consumers and delete custom invocation/package receipts when native status plus ordinary App operation/task events cover the retained user result. |
| Settings diagnostics partition | `five_axis_pointer` | The ten-page Settings partition, redirects, contract, and active AionUI source are implemented. For R1-05 the Contract is aligned and Source is implemented; Pixel, Install, and Release remain unverified and must close independently. There is no pending shell-topic absorption claim. |
| Settings / model policy single source | `policy_authority` | `contracts/app-product-profile.json#codex.auto_model_policy` is the shared App authority for GUI and installer Auto resolution, known overrides, unknown CLI defaults, catalog fallback, and persistence. Settings, AionUI, `opl-native-workbench`, Hermes prior-candidate surfaces, WebUI, and Framework installation may only consume/project it; release cohort, clean-VM, live provider access, and owner acceptance remain separate evidence lanes. |
| Docker WebUI beginner functional path | `non_live_delivery_path` | `contracts/app-install-exposure-policy.json#installer_surfaces.docker_webui.installer_model` now names the beginner non-live path: one-click installer, browser Gateway account login by default with API Key compatibility, shared runtime-provider transport, startup doctor diagnostics, failure recovery, and operator progress surfaces. The automated smoke receipt covers the API Key compatibility route without carrying a real account password. The guide manifest and release-boundary tests validate the contract; this is not release-ready or clean-VM evidence. |
| Local data lifecycle | `five_axis_pointer` | P5 is authoritative: the App contract merges existing Shell-owned updater/conversation/runtime/log categories with two fail-open owner projections, `agent_package_store` and `webui_data_volume`. Framework owner inventory, conditional carrier-host actions, and Shell/Web consumption are source-implemented without the unnecessary generic Docker image/container/build-cache inventory requirement. Package mutation stays on Agents and generic `docker system prune` stays forbidden. |
| Docs lifecycle | `docs_lifecycle` | This file holds current App product gaps and next-round baton; `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, and `docs/decisions.md` hold durable current truth. App `README*` and `docs/**/*.md` docs-governance coverage is compressed in `docs/history/process/README.md`; open App work is release / install / GUI implementation and evidence-tail work under the listed owners, not an unreviewed-docs backlog. |

## Current-State vs Ideal-State Gaps

| Gap | Current state | Completion gate |
| --- | --- | --- |
| `package_platform_composition_migration` | Docs-first target now defines one Official Profile, presence-only dependency edges, native platform lifecycle, dynamic Package/capability discovery, independent silent updates, user-removal persistence, dynamic Home/Runtime, and legacy authority deletion. Phase 2 is executing one family at a time: Official Profile policy/validators, owner-channel currentness and compact Package CLI are already canonical, while additional Framework/App/Shell retirements may exist only as owner-bound source checkpoints until fresh-main absorption. App-state still exposes old lock/version/physical/receipt/materialization compatibility semantics, and real consumers plus most old Manager paths remain migration work. | Continue `W1..W6` from the sole migration plan. For each family require replacement canonical, consumer switch canonical, affected OUT fresh PASS, retained consumers zero, exact physical deletion and same-OUT revalidation. ECO-M0..ECO-M5 are result labels, not a second execution plan. Docs or source checkpoints do not close canonical, Pixel, Install, Release or live lifecycle proof. |
| `role_neutral_package_contributions_v1` | The bounded v1 foundation defines closed declarative navigation, six standard view types, opaque data/action refs, confirmation metadata, role-neutral Framework projection, and a fail-closed Shell parser/resolver. It intentionally does not mount arbitrary plugin code or treat a Package role/executor as navigation authority. | Absorb producer before consumer, mount a real App-owned navigation and standard-view renderer, connect Relay-owned data/actions through the existing state/action boundary, prove invalid-package isolation, then close Pixel, Install, Apple Mail review-path, and Release evidence independently. |
| `runtime_dynamic_agent_core` | Current WorkItemProjection/AionUI route is source implemented under optional `X0-01`, but fixed Agent scope/availability and MAS-specific domain schema remain. | Dynamic installed Agent producers, Agent/Temporal owner split, generic typed-view registry, MAS view, unknown-view local degradation, no Agent-id/schema branches, exact installed behavior, and release evidence all close independently. |
| `claude_science_current_task_boundary` | Conversation current-task and Inspector refs are useful thin consumers; the global Runtime list is conditional X0-01. | Prove current-task context directly from the canonical Work Item slice without requiring the cross-project Runtime route or moving artifact/action/diagnostic authority into the App. |
| `cloud_fabric_x0_core_gate_pruning` | App contracts, page state, profile, fixture, validators, and AionUI source model projection-gated optional resource groups with no default placeholders. Workspace and external groups are independently gated from their canonical projections, with no hosted-platform promise copy. | Preserve the conditional owner-ref boundary and local browser/Docker/WebUI access independently. Close Pixel, Install, and Release without rebuilding hosted scheduling, billing, credential, or execution truth in the App. |
| `private_cross_thread_x0_machine_pruning` | P1c App machine truth and Native main preserve canonical directory/actions plus one App Server adapter while removing the private host, model tools, ledger/receipts, advisory/idempotency, and cross-host parity requirements. | Keep the explicit Native candidate gate green and retain `active_shell_adopted=false`, `release_ready=false`, and all later evidence axes as unverified. Do not rebuild the retired private layer. |
| `b0_remainder_source_audit` | The B0 audit and Source tranche are complete. B0-01..14 are `source_implemented`; B0-03 preserves prompt and attachments across failed sends, B0-10 uses canonical `thread/settings/update.cwd -> thread/read` exact readback before local projection, and B0-14 reuses existing accessibility behavior plus semantic WCAG token regressions. | Keep the public App Server/IPC path, canonical-cwd precedence, projectless failure behavior, and no-rebinding boundary covered. Do not add a second client, private adoption RPC, pending state machine, receipt/Handoff layer, bound-session reassignment, or writable-root mutation. Close B0-10/B0-14 Pixel, Install, and Release independently. |
| `local_data_inventory_completion` | U1-06 Source is implemented: Storage consumes the Framework-owned Agent Package store inventory and carrier-host WebUI data volume lifecycle through the existing App-state/action boundaries. Missing/unknown owner state remains fail-open; Package lifecycle stays on Agents. The conditional WebUI plan/execute/restore ABI uses authenticated bounded JSON endpoints, exact single-use plan confirmation, opaque archive/manifest/receipt refs, idempotent terminal readback or typed conflict, restore proof, and typed non-path errors. | Preserve the owner boundaries and focused coverage. A missing host endpoint keeps only its row status-only and does not block Storage; a complete owner ABI enables only its projected CTA. Do not add another scanner/IPC/lifecycle service in Shell, duplicate package controls in Storage, expose raw host paths, or permit generic Docker pruning. Close Pixel, Install, and Release independently. |
| `active_shell_sync` | App root wrappers prepare App-owned payloads and call the external shell. Current shell source is read from `contracts/app-shell-adapter.json#shell_source.upstream_ref` plus the external `shells/aionui` checkout, and validated through `npm run validate:active-shell -- --quick`, `npm run validate:shell-candidates`, and the compatibility aggregate `npm run validate:shell-convergence`. That aggregate is structure evidence only because it reuses the two owner validators; it does not create an extra readback truth source or claim release/live/adoption state. Stale local shell checkouts are sync tasks; their dated refs, paths, backup folders, and pass/fail transcripts belong in process history or shell-owner evidence, not this active plan. | Product profile and release contracts are generated into the active shell before build/release, without merging shell history into App mainline. A stale local checkout is a sync task, not evidence that the App contract or AionUI mainline failed; release-ready, active-shell-adopted, packaged GUI acceptance, production-ready, real user-path, and Live Evidence claims remain separate evidence gates. |
| `codex_subagent_activity_parity` | Team is correctly off while delegated execution remains available. The ordinary App source consumes existing ACP/App Server metadata, groups canonical child tasks into read-only Active/Done, shows detail/result, opens an existing or on-demand materialized canonical task, and keeps the current conversation usable when read/open fails. | Source is closed with delegated-turn metadata fixtures and focused normalization/DOM tests. Do not add bespoke direct-control buttons, a second client, Team store, scheduler, or shell-owned execution. Close Pixel, Install, and Release independently on the exact candidate bytes. |
| `scheduled_tasks_discoverability` | The carrier-neutral product policy, Scheduled Tasks page state, profile projection, and active AionUI source use one carrier-native scheduler/store. The ordinary Sider entry/section is mounted; create/edit resolves the single generated enabled Codex `Assistant.id`, keeps missing or ambiguous discovery local and fail-open to the rest of the App, and preserves legacy non-Codex jobs. Existing Cron routes cover CRUD, run-now, pause/resume, history, timezone, and focused tests. | Keep the existing AionCore Cron store/routes and fixed-Codex composition. Do not create a second scheduler, store, migration ledger, executor selector, or receipt state machine. Pixel, Install, and Release remain separate. |
| `local_data_lifecycle_issue_5` | Legacy updater cache cleanup is implemented for stale installer packages, including the retired `~/Library/Caches/aionui-updater` root. The broader conversation/runtime/logs growth problem now has contract, active shell service, IPC, Storage UI, and focused test coverage. | Storage panel inventory is visible; conversation cleanup requires archive receipt plus restore proof and explicit user confirmation; runtime cleanup protects current/rollback pointers and only prunes unreferenced roots after dry-run; log rotation removes only log files by bounded policy. Packaged App smoke, real user-directory E2E, release-ready, and owner acceptance remain separate evidence gates. |
| `agui_codex_archived_technical_proof` | `contracts/app-shell-candidates.json` keeps `agui-codex` as `archived_technical_proof`; `contracts/shell-adapters/agui-codex.json` can still explicitly select the linked `shells/agui-codex` external repo for replay. `scripts/validate-shell-candidates.ts` reads the archived proof policy from `alternative_gui_policy`, so default candidate validation covers the registry-selected foreground alternative and AGUI enters validation only through explicit candidate selection. This proves only historical AG-UI/CopilotKit technical learning and remains out of default validation. AGUI-specific React/CopilotKit renderer and WebUI smoke checks are scoped to explicit replay, not to `opl-native-workbench`, Hermes prior reference, or generic shell adoption. The remaining AGUI `candidate` wording is compatibility with App adapter and historical manifests, not current foreground status. | Do not update or improve AGUI unless the user explicitly requests AGUI work. Any replay must stay explicit, preserve release isolation, and avoid domain-ready, production-ready, clean-VM-ready, Full-release-ready, or active-shell-adopted claims. |
| `opl_native_workbench_candidate_gate` | `contracts/app-shell-candidates.json` selects `opl-native-workbench` as the foreground alternative GUI candidate; `contracts/shell-adapters/opl-native-workbench.json` explicitly selects `shells/opl-native-workbench` for technical verification. `docs/product/gui/opl-native-workbench-plan.md` owns the route: candidate registration, adapter, external repo/checkout, state/action bridge, basic UI modules, artifact preview tabs, provenance drawer, starter forms, confirmation/interview cards, desktop/WebUI same renderer, source visual smoke, package manifest, docs/runbook, and later live evidence. K-Dense delivery experience, project sandbox, file/preview/result delivery, structured forms, OpenClaudeScience / Claude Science workbench framing, and AGUI shared-renderer lessons are adopted or adapted only through App-owned contracts and bridge boundaries. External runtime/agent authority, Pi/DeepAgents/LangGraph-like runtimes, provider/backend marketplaces, and domain truth ownership are watch-only or rejected. Hermes remains prior foreground alternative experience rather than the default foreground scope. | Candidate validation must prove registry selection, adapter selection, external checkout readback, App state/action consumption, the non-live product-surface modules, shared renderer when claimed, source visual smoke for the exact candidate cohort, package manifest when packaging is claimed, release isolation, and no release-ready / active-shell-adopted / production-ready claim from docs or contract-only evidence. Default shell adoption still requires a deliberate `contracts/app-shell-adapter.json` change plus App-owned page-state, first-run, product-profile, runtime-bridge, packaged smoke, WebUI-claim, clean-VM, owner acceptance, and release gates. |
| `pilotdeck_reference_intake` | OpenBMB PilotDeck is a reference-only information-organization source. Its evaluated ref, license, reference value, OPL mapping, and forbidden reuse policy are owned by `contracts/app-shell-candidates.json#design_references`, while this plan only keeps the App product gap and intake boundary. The reusable ideas are optional lightweight workspace/session rail, nested session list, chat-first main pane, grouped Files, Skills, Routing, Memory, Always-On context, process traces, and compact composer density. It is AGPL-3.0 and owns its own gateway/runtime/memory/router/always-on state, so it is not an App source dependency or runtime authority. | OPL re-expresses the useful organization patterns in App-owned GUI inventory and selected shell work without making them first-screen panels or a full workbench first screen. The ordinary App path remains chat-first by default, while `contracts/app-shell-candidates.json#design_references` keeps PilotDeck as reference-only with no vendored code, no runtime authority transfer, and no candidate promotion without license and adapter gates. |
| `app_docker_webui_beginner_path` | Docker/WebUI beginner path is now a non-live App contract and guide path: installer entry, browser Gateway account login by default with API Key compatibility, the existing runtime provider, startup diagnostics, failure recovery, and ordinary user progress surfaces are machine-readable in `contracts/app-install-exposure-policy.json` and covered by focused release-boundary checks. | Remaining release proof is intentionally outside this active plan: clean Linux/Windows VM, existing Docker, old data-dir migration, GHCR receipt, and same-cohort user-path artifacts must come from release/evidence lanes before any release-ready claim. |
| `settings_control_plane_unification` | Settings/model/reasoning policy now has a single App-owned control-plane source. `contracts/app-settings-control-plane.json#model_reasoning_policy_source` binds Models and all shell/installer consumers to `app-product-profile#codex.auto_model_policy`; GUI display options and executor policy reference that authority rather than copying the resolver. | Shell implementation and visual QA can still prove rendering parity, but they cannot become a second policy source or replace release/live/provider evidence. |
| `settings_user_cognition_reorg` | The App-owned contract, page-state matrix, validator, product profile, and active AionUI source define seven user-question-first groups over ten stable carrier routes: Overview; Account & Models; Connections & Deployment; Workspace; Agents & Capabilities; Runtime & Maintenance; Preferences. Resources no longer sits under Account & Models; Instructions & Context no longer sits under Workspace; Service Status, Updates & Repair, and Logs & Diagnostics are independent destinations. About is the bottom auxiliary page and Advanced redirects to Logs & Diagnostics. Desktop/WebUI workspace and log mutations follow carrier-specific policies. The startup contract keeps the narrow persisted snapshot, shared background refresh, lazy detail reads, 1,500 ms cold/warm budgets, and 262,144-byte startup projection. | Contract and Source are already closed for R1-05. Remaining gates are exact-feature Pixel, installed launch-to-first-window/Settings-readiness timing and readback, then Release owner cohort evidence; each axis closes independently. |

## Ordered Five-Axis Gap Ledger

This table and `docs/product/gui/shell-conformance-matrix.md` are the only current
five-axis closeout authority. Historical implementation plans and percentage tables
must not be used as a parallel completion ledger. Every row keeps Contract, Source,
Pixel, Install, and Release independent. P0 is docs-only governance, so its five product
axes are explicitly not applicable and it closes no product or evidence claim.

| Order | Scope | Contract | Source | Pixel | Install | Release | Plan and acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0 | Docs vocabulary convergence | `not_applicable` | `not_applicable` | `not_applicable` | `not_applicable` | `not_applicable` | Replace compound or transitional status labels with the five independent axes, keep one Active Truth owner, and remove stale aggregate completion language. Accept only when OPL Doc doctor, local-link check, conflict-marker scan, and targeted stale-vocabulary scan pass on the final bytes. |
| P0b | B0 remainder evidence audit | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | B0-10 performs canonical `thread/settings/update.cwd -> thread/read` exact readback before local projection, rejects bound-session reassignment, treats canonical cwd as authoritative over stale cache, and preserves a projectless conversation on failure. B0-14 Source remains closed through existing keyboard/focus/ARIA/Escape/reduced-motion coverage plus semantic WCAG contrast-token regressions. Close Pixel, Install, and Release independently without reopening Source. |
| P1 | Package platform composition and dynamic Agent Runtime | `target_planned` | `partially_canonical_policy_only` | `not_started` | `not_started` | `not_started` | Phase 2 source retirement is active, while canonical, Pixel, Install and Release outcomes remain independently unclosed. Follow the migration plan's six named work packages and every functionality-equivalence row; ECO-M0..ECO-M5 only name outcomes. Preserve all user outcomes while replacing custom resolver/lock/payload/receipt/materialization with native lifecycle, one Official Profile, dynamic Home/Runtime, Agent/Temporal split, and typed views. |
| P1a | Legacy X0-01 Runtime compatibility bridge | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Keep the current route working during migration. Its optional gate and fixed WorkItem shape are compatibility evidence only; they do not close P1 or authorize deleting the producer before the dynamic core replacement lands. |
| P1b | X0-03/X0-04 Workspace/Fabric/HPC/Console machine-gate pruning | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | App contracts/fixtures/validators and AionUI Resources & Connections expose only projection-gated optional refs. Workspace and external groups mount independently; absent projections create no group, anchor, placeholder, or App-owned scheduling/billing/credential/execution truth. Pixel, Install, and Release stay independent. |
| P1c | X0-05 private cross-thread machine pruning | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | App machine truth and Native main implement the intended single-adapter/no-private-layer boundary. Preserve B0 thread lifecycle and read-only Codex subagent projection, keep the explicit Native gate green, and do not infer active-shell adoption or later evidence from Source. |
| P1d | Role-neutral Package contributions v1 | `aligned_contract_on_task_branch` | `source_foundation_on_task_branches` | `pixel_unverified` | `install_unverified` | `release_unverified` | App owns the closed declarative contract and broker response validation. Framework projects it without role filtering; Shell may use the Framework broker for validated reads, but all writes must enter through the canonical `opl app action execute --action package_contribution_execute` bridge. Until Framework projects that exact action, commands fail closed as hidden/disabled while valid read-only standard views remain available. Canonical absorption, Framework action-bridge implementation, production UI callers, standard-view payload validation, Relay data/action bridging, Apple Mail review-path proof, Pixel, Install, and Release remain open. |
| `legacy-cleanup-alt-carrier-gate` | Candidate default-gate decoupling | `not_applicable` | `not_applicable` | `not_applicable` | `not_applicable` | `not_applicable` | Maintenance slice implemented: default gates validate fixed role registry integrity; Native/Hermes/AGUI detail is explicit on demand; Hermes/AGUI active schema entries are tombstones with adapter/runbook replay refs. Keep focused candidate and final-main release-boundary validation green. This gap-ledger id is unrelated to ecosystem milestones ECO-M0..ECO-M5. |
| P2 | R1-06 branded deep link | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | The only accepted form is hostname-only `opl://navigate?route=...`, with one exact App-owned static or Settings registry path. Cold argv, second-instance, macOS `open-url`, renderer-ready pending pull, invalid-action/payload/secret rejection, redacted reason-only logging, Ubuntu registration, and packaged registration have focused coverage. `aionui://` provider credential/base64 intake is removed; no Source result closes Pixel, Install, or Release. |
| P3 | B0-11 delegated activity UI | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Source is closed: existing ACP/App Server metadata is normalized and deduplicated into read-only Active/Done, detail/result is available, canonical task projections are reused or materialized on demand, and read/open failure is fail-open. No second client, Team store, scheduler, direct-control invention, or Shell execution authority was added; Team stays hidden with no-resurrection coverage. Close exact-feature Pixel, then Install, then Release. |
| P4 | B0-12 Scheduled Tasks | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | AionUI reuses one carrier scheduler/store, mounts ordinary Sider discoverability, composes create/edit with the exact generated enabled Codex identity, keeps missing/ambiguous discovery local, and preserves legacy executor identity. No second scheduler/store/ledger/receipt layer was added; Pixel, Install, and Release remain open. |
| P5 | U1-06 local data lifecycle | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Source is closed: Framework projects the owner inventory, the Web host implements authenticated bounded capability/plan/execute/restore with single-use confirmation, opaque refs, idempotent terminal readback or typed conflict and recovery, and thin Shell/Web consumers preserve fail-open missing capability. The generic Docker inventory burden remains removed; Shell-direct package/path mutation and generic `docker system prune` stay forbidden. Close exact-feature Pixel, installed Desktop/WebUI readback, then Release evidence independently. |
| P6 | U1-05 Docker/WebUI parity | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Source is closed for the current AionUI route: `/settings/storage` is a Web host SPA core route; Desktop alone consumes Electron local lifecycle, while WebUI renders valid Framework/carrier owner projections, omits missing sections, never synthesizes zero bytes or destructive host actions, and refreshes owner inventories independently before a fresh fast-state read. Browser-mode DOM and Web host route tests close Source only. Exact-cohort Pixel, Docker install/readback, clean-VM, and Release evidence remain open. |
| P7a | U1-04 three-object maintenance UX | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Current AionUI distinguishes `opl_base`, `opl_app`, and `opl_packages`; preserve that user vocabulary. P1 may replace the Package mechanics with native adapters without changing the three-object UI. This current Source result does not close P1 or protect old resolver/lock/receipt internals. |
| P7b | Other source-implemented items: `R1-01..05`, `U1-01..03`, `B0-08`, and `B0-13` | `aligned_contract` | `source_implemented` | `pixel_unverified` | `install_unverified` | `release_unverified` | Do not reopen source merely to improve an aggregate score. Close each remaining axis independently with exact-feature pixels, installed readback, then release-owner cohort evidence; one feature's evidence cannot close another's row. |

## Release Claim Boundary

This active plan does not maintain release cohort, clean-VM, packaged `.app` smoke, same-cohort user-path, real user-directory E2E, owner acceptance, or production-ready worklists. Those stay in release artifacts, evidence manifests, validators, workflows, updater metadata, App release records, and owner receipts. This plan may mention them only to preserve false-ready boundaries: App validation, shell tests, contract pass, updater metadata, or refs-only OPL ledger entries do not prove release-ready, active-shell-adopted, domain-ready, or family production-ready claims.

## Next-Round Agent Prompt

Objective:

- Govern the One Person Lab App product docs, functional structure, shell/contract boundaries, and release-evidence routing without moving runtime/provider/domain authority into the App repo.

Write scope:

- `docs/active/app-ideal-state-gap-plan.md`, `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, App contracts, release-boundary validators, testing docs, and focused App tests that affect App product boundary, first-run policy, page-state validation, Settings / Storage, shell-candidate policy, or evidence routing.

Live truth inputs:

- `AGENTS.md`, user-level `~/.codex/TASTE.md`, `README.md`, `docs/README.md`, `docs/status.md`, this plan, App contracts, release-boundary validators, release/test scripts, shell adapter contract, active shell validation outputs, focused shell tests for App-owned GUI behavior, and `npm run validate:agent-installation` output when install exposure is touched. Release evidence manifests are read only for routing and false-ready boundaries unless the task explicitly targets release evidence.
- OPL Framework runtime/App drilldown CLI JSON only as consumed input; it remains framework-owned truth.

Required actions:

- Keep App docs, contracts, release wrappers, evidence manifests, and page-state tests aligned with the App product boundary.
- GUI design work 必须基于 App-owned 定义栈：先读理想交互细则，再读
  Codex-to-OPL delta，再读 feature inventory，最后对照 contracts 和 page-state
  matrices。
- Continue Phase 2 Package platform composition only through the migration plan's six named work packages and bounded owner surfaces: verify canonical Official Profile policy, wire first-run/Restore consumers, migrate per-owner currentness and presence, publish the compact read model, switch Settings/Home, add dynamic Runtime, then delete each legacy family only after replacement proof, consumer zero and same-OUT revalidation. Owner-bound task refs remain checkpoints until fresh-main absorption. Package is the install unit; Skill/Tool/Plugin/MCP/Agent/view capabilities are discovered and presence-checked without an App allowlist.
- Treat `opl-app-contributions.v1` as a bounded W4 source foundation until producer-before-consumer canonical absorption, a real App-owned navigation/standard-view renderer, Relay data/action bridging, invalid-package isolation, Apple Mail review-path proof, and independent Pixel/Install/Release evidence close. Do not infer those results from schema, parser, task branch, or test evidence.
- Preserve the current X0-01 Runtime producer/route as a compatibility bridge while promoting the target Runtime to a core dynamic Agent task surface. Agent owns business lifecycle, Temporal owns execution, Framework joins, and App renders generic fields/`view_kind`; do not add Agent-id or MAS-schema branches.
- Keep surface ownership explicit: Service Status for runtime topology; Updates & Repair for provider/platform repair/update; Logs & Diagnostics for App logs and raw evidence; Agent Package lifecycle in Agents; Skills/Plugins/Flow/local capabilities in Capabilities; user instructions in Instructions & Context; Gateway identity/usage in Account & Access; resource and connector readiness in Connections & Deployment > Resources & Connections; artifact provenance in Inspector; complete same-cohort evidence in release tooling.
- Keep Claude Science / OpenScience learning mapped to owner-produced task awareness: conversation/current-task/right Inspector and the core Runtime consume the same generic projection. Structured results, provenance, ref-level follow-up, capability review, Agent business lifecycle, and Temporal execution keep their existing owners.
- Treat Hosted Workspace/Fabric/HPC/Console as X0-03/X0-04 optional refs/owner routes only. App contracts and active Shell rendering omit absent projections, groups, anchors, and placeholders. App does not own scheduling, billing, credentials, storage execution, or provider truth.
- Preserve the source-implemented U1-06 owner-first path: Framework projects Agent Package store inventory, the carrier host owns the WebUI data volume plan/execute/restore lifecycle, and Shell/Web consume those owner surfaces without duplicating them. Keep absent owner projections fail-open, route Package lifecycle to Agents, submit only opaque action/plan/confirmation/receipt refs, require fresh terminal readback and recovery, and never mutate package/path state directly from Shell or route generic `docker system prune` as product cleanup. Close Pixel, Install, and Release independently.
- Preserve one Storage product route across carriers without inventing parity infrastructure: Desktop may consume the existing Electron local lifecycle; WebUI must not invoke it and instead renders only valid owner projections from fast App state. When WebUI has no valid storage projection and no explicit error, present a neutral explanation that the browser deployment is not connected to storage statistics, state that existing data and other features are unaffected, omit Retry, and offer only `View deployment status`; permission, service, IPC, and unknown operational errors remain warning states with proportionate retry or recovery actions. Missing or failed owner refreshes keep `/settings/storage` usable, unknown capacity remains unavailable rather than zero, and `host_action_required` remains status-only until the carrier host projects a real executable action. User-facing copy must not expose implementation terms such as desktop storage carrier, owner projection, or carrier host. Source/DOM/route tests do not close Pixel, Docker install, clean-VM, or Release axes.
- Replace the independent agent installation contract with one minimum Package/capability descriptor and carrier-backed installed discovery. Current first-party Agent examples are `kind=agent`, but the App owns no fixed inventory or count. MAS Scholar Skills is MAS's owner-declared required capability Package; no other Package inherits that edge without its own owner contract. Standard/Full consume one Official Profile; user removal persists. Preserve Install/Update/Enable/Show/Uninstall/Home outcomes while retiring registry resolver, lock, payload, receipt, materialization and rollback compatibility fields after zero-consumer proof.
- Keep the App ordinary path aligned with the Codex App equivalent positioning: Codex CLI fixed executor, no home or ordinary-conversation backend/provider selector, configurable professional-agent shortcuts over installed packages, and App-owned model/reasoning plus user-language permission/access controls at the composer decision point.
- Keep AionUI Team and Codex subagents on separate axes. Team remains disabled; delegated execution uses the existing runtime and adapter. The source-implemented portable core is limited to read-only Active/Done lists, completed detail/result, canonical task opening, and existing owner-supported controls. Do not add bespoke direct-control buttons, a second client, Team store, scheduler, or shell-owned execution authority; close Pixel, Install, and Release separately.
- Keep user-triggered thread operations on the single existing App Server adapter. Treat private coordination host/model tools/ledger/receipts/advisory/cross-host parity as X0-05 no-build material; P1c keeps those machine hard gates and the Native private source absent without changing the current adapter.
- Treat the B0 Source audit as complete while keeping axes independent: B0-01..14 are reused or narrowly completed `source_implemented` behavior. Route project-adoption pixels, B0-14 screen-reader/keyboard/rendered-contrast checks, Install, and Release to their owning evidence axes. Do not create duplicate parity code or a second accessibility layer.
- Keep Scheduled Tasks on the existing Cron implementation and fixed-Codex composition; do not introduce a second scheduler, executor selector, or duplicate Cron state. Close only its remaining Pixel, Install, and Release axes.
- Keep Codex CLI conversation output rendering aligned with native Codex behavior: ACP tool-call normalization must preserve newline-bearing `raw_output` / `stdout` / `stderr` content instead of collapsing command output into one visual line.
- Keep professional-agent launch observable through ordinary App task events or bounded, non-authoritative operation feedback derived from the owner result. Treat the custom invocation receipt as migration compatibility and remove it if no retained consumer needs more than selected Package, launch result, and task identity.
- Keep ordinary Settings aligned with the App-owned seven-group navigation over ten stable carrier routes: Overview; Account & Models; Connections & Deployment; Workspace; Agents & Capabilities; Runtime & Maintenance; Preferences. Keep About as the bottom auxiliary page, redirect Advanced to Logs & Diagnostics, and keep Update/Theme/Local Services/Personalization as owner-anchor compatibility routes. Preserve the narrow startup snapshot, shared background refresh, lazy detail reads, 1,500 ms cold/warm budgets, and 262,144-byte startup projection.
- Keep shell candidate work isolated in `contracts/app-shell-candidates.json`, `contracts/shell-adapters/<candidate>.json`, and linked `shells/<candidate>` external repos until adoption; default release wrappers continue to use `contracts/app-shell-adapter.json` unless `OPL_APP_SHELL_ADAPTER_CONTRACT` is explicitly set for a candidate build.
- `legacy-cleanup-alt-carrier-gate` canonical bytes keep only fixed GUI roles/minimal registry integrity in default AionUI and release gates and move Native/Hermes/AGUI detail validation to explicit on-demand commands. Preserve this split: untouched candidate detail must not block the active shell, while role tombstones, adapters, runbooks, and explicit validators remain available.
- Keep AG-UI/CopilotKit material archived as technical proof only. Do not update or improve AGUI unless the user explicitly requests AGUI work; explicit replay still uses `docs/history/shell-candidates/agui-codex-candidate-verification.md`, the AGUI adapter contract, and release-isolated smoke artifacts.
- Keep `opl-native-workbench` candidate docs aligned with `docs/product/gui/opl-native-workbench-plan.md`: the target is an App-owned foreground alternative route with candidate registration, adapter, independent external checkout, state/action bridge, basic UI modules, artifact preview tabs, provenance drawer, starter forms, confirmation/interview cards, desktop/WebUI same renderer, source visual smoke, package manifest, docs/runbook, and later live evidence. Keep Hermes docs as prior foreground alternative reference only; do not use them as the default foreground implementation scope.
- Use PilotDeck only as a design reference for information organization and navigation shape. Re-express useful ideas as OPL-owned lightweight workspace/session rail and right-side collapsible inspector requirements; do not copy AGPL source, adopt PilotDeck runtime authority, expose its provider/model/router controls, or make a full workbench first screen on the ordinary App path.
- Keep release cohort evidence out of this active gap list. When release work is explicitly requested, classify evidence in the release-owned manifests as present, missing, typed blocker, or not applicable; never promote missing evidence to release-ready proof.
- Route retired release workflow no-resurrection checks to `scripts/validate-release-boundary.ts`, focused release-boundary behavior tests, and `docs/history/process/retired-surface-provenance.md`; this active plan should only carry release/evidence gaps that need the next App owner action.
- When Maintenance diagnostics or release tooling collects operator/full-state refs for a current cohort, route them only to release evidence manifests; do not expose them in Runtime or convert them into release-ready, domain-ready, or production-ready authority in this active plan.
- Keep active shell intake explicit: App-owned product/release contract changes stay here, shell implementation changes stay in `opl-aion-shell`.

Non-goals:

- Do not own OPL runtime truth, provider implementation, action-route authority, domain truth, domain quality/export verdict, memory body, artifact body, artifact authority, owner receipt authority, or shell implementation history.
- Do not infer missing Codex subagent capability from disabled AionUI Team, or use subagent display parity to justify a second coordination/runtime control plane.
- Do not use App UI rendering, provider completion, updater metadata, or release artifact existence as proof of MAS/MAG/RCA/BookForge readiness or family production readiness.

Verification commands:

- Docs-only: `rtk git diff --check`, `rtk rg -n "^(<<<<<<<|=======|>>>>>>>)" docs`, `python3 ../opl-doc/scripts/opl_doc_doctor.py doctor . --format json`.
- App contract/release boundary changes: `rtk npm run test:release-boundary`, `rtk npm run validate:agent-installation`, live `npm run validate:agent-installation -- --codex-skills-root ~/.codex/skills --agent-root mas=<path> --agent-root mag=<path> --agent-root rca=<path>` when plugin roots are available, `rtk node --experimental-strip-types scripts/validate-active-shell.ts --quick`, and the touched release/evidence validation script.
- AG-UI/CopilotKit explicit replay only when requested: `rtk npm run validate:shell-candidates -- --candidate agui-codex`, `rtk env OPL_APP_SHELL_ADAPTER_CONTRACT=contracts/shell-adapters/agui-codex.json node --experimental-strip-types scripts/validate-active-shell.ts --quick`, and shell/package smoke commands only when AGUI evidence is explicitly being replayed.
- Shell convergence aggregate: `rtk npm run validate:shell-convergence`.
- `opl-native-workbench` candidate contract/docs changes: `rtk npm run validate:shell-candidates -- --candidate opl-native-workbench`, adapter/readback checks from `contracts/shell-adapters/opl-native-workbench.json`, focused source tests in `shells/opl-native-workbench` when available, and, when packaging evidence is claimed, `rtk npm run validate:shell-candidates -- --candidate opl-native-workbench --run-candidate-commands`. If `shells/opl-native-workbench` is absent, report `blocked_missing_checkout` rather than claiming source, WebUI, package, visual, live, or release evidence.
- Shell implementation changes: focused `vitest` for Guid/home/conversation selectors, Settings modal, Guid Access shortcut, and route receipt, `bunx tsc --noEmit`, `bunx oxfmt --check` on touched shell files, and packaged GUI smoke with `--assistant-route-smoke` for release evidence.

Completion gate:

- The App active plan, status, project, architecture, invariants, decisions, contracts, and test docs agree on App ownership and non-ownership.
- Package installation has one semantic path: native platform lifecycle plus Framework installed discovery/presence aggregation. No App-owned Package/Agent/Skill/Tool/Plugin allowlist or duplicate mirror remains.
- Home, ordinary conversation, compact Settings, dynamic Runtime, Agent/Temporal split, typed views, user-removal persistence, and Package-local failure are enforced independently by contracts, source, installed readback, and release evidence.
- Team remains disabled without suppressing real Codex subagent events; read-only activity/detail/open-thread and any existing owner-supported control claim is backed by a real delegated-turn fixture, with Pixel/Install/Release reported separately.
- Current docs agree that X0-05 private coordination is no-build; any remaining candidate contract/validator/test requirement is recorded as P1c machine deviation rather than product or release necessity.
- Release evidence gaps are explicit; packaged assistant route smoke must be present before claiming packaged GUI Codex-path evidence, and no App doc claims runtime truth, domain ready, release ready, or production ready beyond the available artifacts and contracts.

Foldback target:

- Durable product truth folds back to `docs/status.md`, `docs/project.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/decisions.md`, or App contracts.
- Release proof, screenshots, VM logs, remote verification, and command traces stay in release artifacts, evidence manifests, CI logs, history docs, or commit history, not in active docs as execution logs.
