name: OPL Stable Desktop Release Set Certification
run-name: ${{ github.event_name == 'workflow_dispatch' && format('Existing additive repair verification for follow-up {0}', inputs.followup_run_id) || format('Desktop Release Set certification for follow-up {0}', github.event.workflow_run.id) }}

on:
  workflow_run:
    workflows:
      - OPL Stable Follow-ups
    types:
      - completed
  workflow_dispatch:
    inputs:
      operation:
        description: Protected read-only verification of an already-published additive repair.
        required: true
        type: choice
        options:
          - verify_existing_repair
      source_run_id:
        description: Exact successful Stable source run that published the original Release Set.
        required: true
        type: string
      followup_run_id:
        description: Exact successful additive repair follow-up run whose public receipt is consumed.
        required: true
        type: string
      verification_source_commit:
        description: Exact canonical App main commit containing this certification fix.
        required: true
        type: string
      operator_confirmation:
        description: Type VERIFY EXISTING ADDITIVE REPAIR to authorize read-only certification.
        required: true
        type: string

permissions:
  contents: read
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: opl-desktop-release-set-certification-${{ github.event_name == 'workflow_dispatch' && inputs.followup_run_id || github.event.workflow_run.id }}
  cancel-in-progress: false

jobs:
  resolve-release-set:
    if: ${{ (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main') || (github.event_name == 'workflow_dispatch' && inputs.operation == 'verify_existing_repair') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      applicable: ${{ steps.authority.outputs.applicable }}
      reason_code: ${{ steps.authority.outputs.reason_code }}
      certification_kind: ${{ steps.authority.outputs.certification_kind }}
      certify_macos: ${{ steps.authority.outputs.certify_macos }}
      source_run_id: ${{ steps.authority.outputs.source_run_id }}
      followup_run_id: ${{ steps.authority.outputs.followup_run_id }}
      tag: ${{ steps.identity.outputs.tag || steps.repair_identity.outputs.tag }}
      app_sha: ${{ steps.identity.outputs.app_sha || steps.repair_identity.outputs.app_sha }}
      shell_sha: ${{ steps.identity.outputs.shell_sha || steps.repair_identity.outputs.shell_sha }}
      framework_sha: ${{ steps.identity.outputs.framework_sha || steps.repair_identity.outputs.framework_sha }}
      component_manifest_digest: ${{ steps.identity.outputs.component_manifest_digest || steps.repair_identity.outputs.component_manifest_digest }}
      installer_name: ${{ steps.identity.outputs.installer_name || steps.repair_identity.outputs.installer_name }}
      installer_digest: ${{ steps.identity.outputs.installer_digest || steps.repair_identity.outputs.installer_digest }}
      previous_installer_digest: ${{ steps.repair_identity.outputs.previous_installer_digest }}
      standard_artifact_name: ${{ steps.identity.outputs.standard_artifact_name || steps.repair_identity.outputs.standard_artifact_name }}
      standard_artifact_digest: ${{ steps.identity.outputs.standard_artifact_digest || steps.repair_identity.outputs.standard_artifact_digest }}
      linux_artifact_name: ${{ steps.identity.outputs.linux_artifact_name || steps.repair_identity.outputs.linux_artifact_name }}
      linux_artifact_digest: ${{ steps.identity.outputs.linux_artifact_digest || steps.repair_identity.outputs.linux_artifact_digest }}
    steps:
      - name: Checkout exact certification source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.verification_source_commit || github.event.workflow_run.head_sha }}
          fetch-depth: 1

      - name: Admit one completed Desktop Release Set follow-up
        id: authority
        env:
          GH_TOKEN: ${{ github.token }}
          FOLLOWUP_RUN_ID: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.id || '' }}
          INPUT_OPERATION: ${{ inputs.operation }}
          INPUT_SOURCE_RUN_ID: ${{ inputs.source_run_id }}
          INPUT_FOLLOWUP_RUN_ID: ${{ inputs.followup_run_id }}
          VERIFICATION_SOURCE_COMMIT: ${{ inputs.verification_source_commit }}
          OPERATOR_CONFIRMATION: ${{ inputs.operator_confirmation }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" = workflow_dispatch ]; then
            test "$INPUT_OPERATION" = verify_existing_repair
            test "$OPERATOR_CONFIRMATION" = 'VERIFY EXISTING ADDITIVE REPAIR'
            SOURCE_RUN_ID="$INPUT_SOURCE_RUN_ID"
            FOLLOWUP_RUN_ID="$INPUT_FOLLOWUP_RUN_ID"
            [[ "$SOURCE_RUN_ID" =~ ^[1-9][0-9]*$ ]]
            [[ "$FOLLOWUP_RUN_ID" =~ ^[1-9][0-9]*$ ]]
            [[ "$VERIFICATION_SOURCE_COMMIT" =~ ^[0-9a-f]{40}$ ]]
            test "$(git rev-parse HEAD)" = "$VERIFICATION_SOURCE_COMMIT"
            gh api "repos/$GITHUB_REPOSITORY/branches/main" > current-main.json
            test "$(jq -er .commit.sha current-main.json)" = "$VERIFICATION_SOURCE_COMMIT"
            gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_RUN_ID" > source-run.json
            jq -e --arg run "$SOURCE_RUN_ID" '
              (.id | tostring) == $run
              and .repository.full_name == "gaofeng21cn/one-person-lab-app"
              and .head_repository.full_name == "gaofeng21cn/one-person-lab-app"
              and .path == ".github/workflows/release-stable.yml"
              and .event == "workflow_dispatch"
              and .head_branch == "main"
              and .run_attempt == 1
              and .status == "completed"
              and .conclusion == "success"
            ' source-run.json >/dev/null
            gh api "repos/$GITHUB_REPOSITORY/actions/runs/$FOLLOWUP_RUN_ID" > followup-run.json
            jq -e --arg run "$FOLLOWUP_RUN_ID" --arg source "$SOURCE_RUN_ID" '
              (.id | tostring) == $run
              and .repository.full_name == "gaofeng21cn/one-person-lab-app"
              and .head_repository.full_name == "gaofeng21cn/one-person-lab-app"
              and .path == ".github/workflows/release-stable-post-success-followups.yml"
              and .event == "workflow_dispatch"
              and .head_branch == "main"
              and .run_attempt == 1
              and .status == "completed"
              and .conclusion == "success"
              and (.display_title | test("^Stable additive repair for source run " + $source + "$"))
            ' followup-run.json >/dev/null
            gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/runs/$FOLLOWUP_RUN_ID/artifacts?per_page=100" > followup-artifacts.json
            jq -e --arg name "opl-stable-additive-repair-$SOURCE_RUN_ID" '
              [.[]?.artifacts[]? | select(.name == $name and .expired == false)] | length == 1
            ' followup-artifacts.json >/dev/null
            {
              echo "applicable=true"
              echo "reason_code=existing_repair_verification"
              echo "certification_kind=repair_additive"
              echo "certify_macos=false"
              echo "source_run_id=$SOURCE_RUN_ID"
              echo "followup_run_id=$FOLLOWUP_RUN_ID"
              echo "operation_kind=standard"
              echo "repair_receipt=opl-stable-additive-repair-$SOURCE_RUN_ID"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          test "$GITHUB_RUN_ATTEMPT" = 1
          [[ "$FOLLOWUP_RUN_ID" =~ ^[1-9][0-9]*$ ]]
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$FOLLOWUP_RUN_ID" > followup-run.json
          jq -e --arg run "$FOLLOWUP_RUN_ID" '
            (.id | tostring) == $run
            and .repository.full_name == "gaofeng21cn/one-person-lab-app"
            and .head_repository.full_name == "gaofeng21cn/one-person-lab-app"
            and .path == ".github/workflows/release-stable-post-success-followups.yml"
            and .head_branch == "main"
            and .run_attempt == 1
            and .status == "completed"
            and (
              (.event == "workflow_run" and (.display_title | test("^Stable follow-ups for Stable run [1-9][0-9]*$")))
              or (.event == "workflow_dispatch" and (
                (.display_title | test("^Stable additive repair for source run [1-9][0-9]*$"))
                or (.display_title | test("^Desktop (linux-x64|windows-x64) reconcile for Stable run [1-9][0-9]*$"))
                or (.display_title | test("^Stable follow-up (reconcile_full_addon|reconcile_homebrew_standard|reconcile_homebrew_full) for source run [1-9][0-9]*$"))
              ))
            )
          ' followup-run.json >/dev/null
          followup_run_id="$FOLLOWUP_RUN_ID"
          if jq -e '.display_title | test("^Stable additive repair for source run ")' followup-run.json >/dev/null; then
            source_run_id="$(jq -er '.display_title | capture("source run (?<run>[1-9][0-9]*)$").run' followup-run.json)"
            certification_kind=repair_additive
          elif jq -e '.display_title | test("^Stable follow-up ")' followup-run.json >/dev/null; then
            source_run_id="$(jq -er '.display_title | capture("source run (?<run>[1-9][0-9]*)$").run' followup-run.json)"
            certification_kind=not_applicable
          else
            source_run_id="$(jq -er '.display_title | capture("Stable run (?<run>[1-9][0-9]*)$").run' followup-run.json)"
            certification_kind=desktop_release_set
          fi
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$source_run_id" > source-run.json
          jq -e --arg run "$source_run_id" '
            (.id | tostring) == $run
            and .repository.full_name == "gaofeng21cn/one-person-lab-app"
            and .head_repository.full_name == "gaofeng21cn/one-person-lab-app"
            and .path == ".github/workflows/release-stable.yml"
            and .event == "workflow_dispatch"
            and .head_branch == "main"
            and .run_attempt == 1
            and .status == "completed"
          ' source-run.json >/dev/null
          if [ "$(jq -er .conclusion source-run.json)" != success ]; then
            {
              echo "applicable=false"
              echo "reason_code=source_run_not_successful"
              echo "source_run_id=$source_run_id"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          gh run download "$FOLLOWUP_RUN_ID" --repo "$GITHUB_REPOSITORY" \
            --name "opl-stable-followup-route-$source_run_id" --dir followup-route
          mapfile -t routes < <(find followup-route -type f -name stable-followup-route.json -print | LC_ALL=C sort)
          test "${#routes[@]}" = 1
          jq -e '.schema == "opl_app_stable_followup_route.v1"' "${routes[0]}" >/dev/null
          source_operation="$(jq -er '.source_operation | strings' "${routes[0]}")"
          case "$source_operation" in
            standard|resume_standard) ;;
            append_full) reason_code=source_operation_append_full ;;
            *) reason_code=source_operation_not_desktop_release_set ;;
          esac
          if [ "$source_operation" != standard ] && [ "$source_operation" != resume_standard ]; then
            {
              echo "applicable=false"
              echo "reason_code=$reason_code"
              echo "source_run_id=$source_run_id"
              echo "followup_run_id=$FOLLOWUP_RUN_ID"
              echo "operation_kind=$source_operation"
              echo "certification_kind=$certification_kind"
              echo "certify_macos=false"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          gh run download "$source_run_id" --repo "$GITHUB_REPOSITORY" \
            --name "opl-release-operation-admission-$source_run_id" --dir source-operation-admission
          mapfile -t admissions < <(find source-operation-admission -type f -name release-operation-admission.json -print | LC_ALL=C sort)
          test "${#admissions[@]}" = 1
          operation_kind="$(jq -er '.operation | strings' "${admissions[0]}")"
          test "$operation_kind" = "$source_operation"
          gh api --paginate --slurp \
            "repos/$GITHUB_REPOSITORY/actions/runs/$FOLLOWUP_RUN_ID/artifacts?per_page=100" \
            > followup-artifacts.json
          case "$operation_kind" in
            standard|resume_standard) ;;
            append_full) reason_code=source_operation_append_full ;;
            *) reason_code=source_operation_not_desktop_release_set ;;
          esac
          if [ "$operation_kind" != standard ] && [ "$operation_kind" != resume_standard ]; then
            {
              echo "applicable=false"
              echo "reason_code=$reason_code"
              echo "source_run_id=$source_run_id"
              echo "followup_run_id=$FOLLOWUP_RUN_ID"
              echo "operation_kind=$operation_kind"
              echo "certification_kind=$certification_kind"
              echo "certify_macos=false"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$certification_kind" = not_applicable ]; then
            {
              echo "applicable=false"
              echo "reason_code=followup_lane_not_certifiable"
              echo "source_run_id=$source_run_id"
              echo "followup_run_id=$FOLLOWUP_RUN_ID"
              echo "operation_kind=$operation_kind"
              echo "certification_kind=$certification_kind"
              echo "certify_macos=false"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$certification_kind" = repair_additive ]; then
            for name in "opl-stable-additive-repair-plan-${source_run_id}" "opl-stable-additive-repair-${source_run_id}"; do
              jq -e --arg name "$name" '[.[]?.artifacts[]? | select(.name == $name and .expired == false)] | length == 1' \
                followup-artifacts.json >/dev/null
            done
            {
              echo "applicable=true"
              echo "reason_code=stable_additive_repair"
              echo "certification_kind=repair_additive"
              echo "certify_macos=false"
              echo "source_run_id=$source_run_id"
              echo "operation_kind=$operation_kind"
              echo "repair_receipt=opl-stable-additive-repair-${source_run_id}"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          jq -e --arg name "opl-stable-release-set-followup-${source_run_id}" \
            '[.[]?.artifacts[]? | select(.name == $name and .expired == false)] | length == 1' \
            followup-artifacts.json >/dev/null
          linux_receipt="opl-stable-desktop-append-${source_run_id}-linux-x64"
          linux_count="$(jq -r --arg name "$linux_receipt" '[.[]?.artifacts[]? | select(.name == $name and .expired == false)] | length' followup-artifacts.json)"
          test "$linux_count" -le 1
          if [ "$linux_count" = 0 ]; then
            {
              echo "applicable=false"
              echo "reason_code=linux_platform_not_completed_in_followup"
              echo "certification_kind=desktop_release_set"
              echo "certify_macos=false"
              echo "source_run_id=$source_run_id"
              echo "followup_run_id=$followup_run_id"
              echo "operation_kind=$operation_kind"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          {
            echo "applicable=true"
            echo "reason_code=desktop_release_set_followup"
            echo "certification_kind=desktop_release_set"
            echo "certify_macos=true"
            echo "source_run_id=$source_run_id"
            echo "followup_run_id=$followup_run_id"
            echo "operation_kind=$operation_kind"
            echo "followup_receipt=opl-stable-release-set-followup-${source_run_id}"
            echo "desktop_receipt=$linux_receipt"
          } >> "$GITHUB_OUTPUT"

      - name: Download exact Release Set follow-up receipt
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'desktop_release_set' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ steps.authority.outputs.followup_receipt }}
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
          path: followup-receipt

      - name: Download exact Desktop append receipt
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'desktop_release_set' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ steps.authority.outputs.desktop_receipt }}
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
          path: desktop-receipt

      - name: Bind completed follow-up identity
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'desktop_release_set' }}
        id: followup
        env:
          SOURCE_RUN_ID: ${{ steps.authority.outputs.source_run_id }}
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t receipts < <(find followup-receipt -type f -name stable-release-set-followup.json -print)
          test "${#receipts[@]}" = 1
          receipt="${receipts[0]}"
          jq -e --arg source "$SOURCE_RUN_ID" '
            .schema == "opl_app_stable_desktop_followup.v1"
            and .source_run_id == $source
            and (.operation_kind == "standard" or .operation_kind == "resume_standard")
            and (.release_tag | test("^v[0-9]+\\.[0-9]+\\.[0-9]+(?:-r[1-9][0-9]*)?$"))
            and (.platforms | index("linux-x64") != null)
            and .platform_reconcile == "success"
            and .required_for_standard_or_latest == false
            and .remaining == []
          ' "$receipt" >/dev/null
          {
            echo "source_run_id=$SOURCE_RUN_ID"
            echo "tag=$(jq -er .release_tag "$receipt")"
          } >> "$GITHUB_OUTPUT"

      - name: Bind one public Desktop Release Set
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'desktop_release_set' }}
        id: identity
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_RUN_ID: ${{ steps.followup.outputs.source_run_id }}
          EXPECTED_TAG: ${{ steps.followup.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t append_receipts < <(find desktop-receipt -type f -name stable-desktop-linux-x64-append.json -print)
          test "${#append_receipts[@]}" = 1
          append_receipt="${append_receipts[0]}"
          jq -e --arg tag "$EXPECTED_TAG" '
            .schema == "opl_app_stable_desktop_asset_append.v1"
            and .status == "complete"
            and .release.tag == $tag
            and .release.draft == false
            and .release.prerelease == false
            and (.release.target_commitish | test("^[0-9a-f]{40}$"))
            and .platform == "linux-x64"
            and .remaining == []
            and ([.assets[].name] | length == (unique | length))
            and ([.assets[].name] | index("opl-desktop-platforms-manifest.json") != null)
          ' "$append_receipt" >/dev/null
          app_sha="$(jq -er .release.target_commitish "$append_receipt")"
          release_id="$(jq -er .release.id "$append_receipt")"
          gh api "repos/$GITHUB_REPOSITORY/releases/$release_id" > public-release.json
          jq -e --argjson id "$release_id" --arg tag "$EXPECTED_TAG" --arg target "$app_sha" '
            .id == $id and .tag_name == $tag and .target_commitish == $target
            and .draft == false and .prerelease == false and .immutable == false
            and ([.assets[].name] | length == (unique | length))
          ' public-release.json >/dev/null
          release_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${EXPECTED_TAG}"
          curl --fail --location --retry 0 "$release_base/opl-app-component-manifest.json" --output component-manifest.json
          curl --fail --location --retry 0 "$release_base/opl-desktop-platforms-manifest.json" --output desktop-manifest.json
          node --experimental-strip-types scripts/read-opl-app-component-manifest-identity.ts \
            --manifest component-manifest.json --expected-tag "$EXPECTED_TAG" \
            --release-prerelease false --expected-source-commit "$app_sha" > component-identity.json
          shell_sha="$(jq -er .cohort.shell_sha desktop-manifest.json)"
          framework_sha="$(jq -er .cohort.framework_sha desktop-manifest.json)"
          jq -e --arg source "$SOURCE_RUN_ID" --arg app "$app_sha" --arg shell "$shell_sha" --arg framework "$framework_sha" '
            .schema == "opl_app_desktop_release_set_manifest.v1"
            and .source.run_id == $source
            and .cohort == {app_sha:$app,shell_sha:$shell,framework_sha:$framework}
            and .platforms == ["linux-x64","windows-x64"]
            and ([.assets[].name] | length == (unique | length))
          ' desktop-manifest.json >/dev/null
          desktop_manifest_digest="sha256:$(sha256sum desktop-manifest.json | awk '{print $1}')"
          jq -e --arg digest "$desktop_manifest_digest" '
            [.assets[] | select(.name == "opl-desktop-platforms-manifest.json" and .digest == $digest)] | length == 1
          ' public-release.json >/dev/null
          jq -e --argjson expected "$(jq .assets desktop-manifest.json)" '
            . as $release | all($expected[]; . as $asset | any($release.assets[]; .name == $asset.name and .digest == $asset.digest and .size == $asset.size))
          ' public-release.json >/dev/null
          version="${EXPECTED_TAG#v}"
          standard_name="One-Person-Lab-${version}-mac-arm64.dmg"
          linux_name="One-Person-Lab-${version}-linux-x64.deb"
          installer_name=opl-install.sh
          standard_digest="$(jq -er --arg name "$standard_name" '[.assets[] | select(.name == $name)] | select(length == 1) | .[0].digest' public-release.json)"
          linux_digest="$(jq -er --arg name "$linux_name" '[.assets[] | select(.name == $name)] | select(length == 1) | .[0].digest' public-release.json)"
          installer_digest="$(jq -er --arg name "$installer_name" '[.assets[] | select(.name == $name)] | select(length == 1) | .[0].digest' public-release.json)"
          {
            echo "tag=$EXPECTED_TAG"
            echo "app_sha=$app_sha"
            echo "shell_sha=$shell_sha"
            echo "framework_sha=$framework_sha"
            echo "component_manifest_digest=$(jq -er .component_manifest_digest component-identity.json)"
            echo "installer_name=$installer_name"
            echo "installer_digest=$installer_digest"
            echo "standard_artifact_name=$standard_name"
            echo "standard_artifact_digest=$standard_digest"
            echo "linux_artifact_name=$linux_name"
            echo "linux_artifact_digest=$linux_digest"
          } >> "$GITHUB_OUTPUT"

      - name: Download exact additive repair receipt
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'repair_additive' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ steps.authority.outputs.repair_receipt }}
          run-id: ${{ steps.authority.outputs.followup_run_id }}
          github-token: ${{ github.token }}
          path: repair-receipt

      - name: Download original Stable source bundle
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'repair_additive' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: opl-release-bundle-${{ steps.authority.outputs.source_run_id }}
          run-id: ${{ steps.authority.outputs.source_run_id }}
          github-token: ${{ github.token }}
          path: repair-source-bundle

      - name: Bind public additive repair and installer digest chain
        if: ${{ steps.authority.outputs.applicable == 'true' && steps.authority.outputs.certification_kind == 'repair_additive' }}
        id: repair_identity
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_RUN_ID: ${{ steps.authority.outputs.source_run_id }}
          REPAIR_SOURCE_COMMIT: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || '' }}
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t receipts < <(find repair-receipt -type f -name 'opl-additive-repair-*.json' -print)
          mapfile -t bundles < <(find repair-source-bundle -type f -name release-bundle.json -print)
          test "${#receipts[@]}" = 1
          test "${#bundles[@]}" = 1
          receipt="${receipts[0]}"
          bundle="${bundles[0]}"
          jq -e --arg source "$SOURCE_RUN_ID" '
            .schema == "opl_app_stable_additive_repair.v1"
            and .status == "complete"
            and .source_run_id == $source
            and .release.draft == false
            and .release.prerelease == false
            and (.release.id | type == "number")
            and (.release.tag | test("^v[0-9]+\\.[0-9]+\\.[0-9]+(?:-r[1-9][0-9]*)?$"))
            and (.release.target_commitish | test("^[0-9a-f]{40}$"))
            and (.release.body_digest | test("^sha256:[0-9a-f]{64}$"))
            and (.frozen_assets | length == 5)
            and ([.frozen_assets[].name] | length == (unique | length))
            and .replacement.previous.name == "opl-install.sh"
            and .replacement.next.name == "opl-install.sh"
            and (.replacement.previous.id | type == "number")
            and (.replacement.previous.size | type == "number")
            and (.replacement.previous.digest | test("^sha256:[0-9a-f]{64}$"))
            and (.replacement.next.size | type == "number")
            and (.replacement.next.digest | test("^sha256:[0-9a-f]{64}$"))
            and .replacement.previous.digest != .replacement.next.digest
            and (.public_receipt | test("^opl-additive-repair-[0-9a-f]{12}\\.json$"))
            and .remaining == []
          ' "$receipt" >/dev/null
          receipt_repair_source="$(jq -er .repair_source_commit "$receipt")"
          if [ "$GITHUB_EVENT_NAME" = workflow_run ]; then
            test "$receipt_repair_source" = "$REPAIR_SOURCE_COMMIT"
          else
            [[ "$receipt_repair_source" =~ ^[0-9a-f]{40}$ ]]
          fi
          tag="$(jq -er .release.tag "$receipt")"
          release_id="$(jq -er .release.id "$receipt")"
          app="$(jq -er .release.target_commitish "$receipt")"
          expected_body="$(jq -er .release.body_digest "$receipt")"
          public_receipt_name="$(jq -er .public_receipt "$receipt")"
          jq -e --arg tag "$tag" --arg app "$app" '
            .release.tag == $tag
            and .sources.app.source_commit == $app
            and (.sources.shell.source_commit | test("^[0-9a-f]{40}$"))
            and (.sources.framework.source_commit | test("^[0-9a-f]{40}$"))
          ' "$bundle" >/dev/null
          shell="$(jq -er .sources.shell.source_commit "$bundle")"
          framework="$(jq -er .sources.framework.source_commit "$bundle")"
          gh api "repos/$GITHUB_REPOSITORY/releases/$release_id" > public-release.json
          gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$tag" > public-tag.json
          jq -e --arg tag "$tag" --arg app "$app" '
            .ref == ("refs/tags/" + $tag) and .object.type == "commit" and .object.sha == $app
          ' public-tag.json >/dev/null
          jq -e --argjson id "$release_id" --arg tag "$tag" --arg app "$app" '
            .id == $id and .tag_name == $tag and .target_commitish == $app
            and .draft == false and .prerelease == false and .immutable == false
            and ([.assets[].name] | length == (unique | length))
          ' public-release.json >/dev/null
          test "sha256:$(jq -jr .body public-release.json | sha256sum | awk '{print $1}')" = "$expected_body"
          jq -e --argjson receipt "$(jq . "$receipt")" '
            . as $release
            | all($receipt.frozen_assets[]; . as $asset | any($release.assets[]; .name == $asset.name and .size == $asset.size and .digest == $asset.digest))
            and ([.assets[] | select(.name == "opl-install.sh" and .size == $receipt.replacement.next.size and .digest == $receipt.replacement.next.digest)] | length == 1)
            and ([.assets[] | select(.name == $receipt.public_receipt)] | length == 1)
          ' public-release.json >/dev/null
          release_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${tag}"
          curl --fail --location --retry 0 "$release_base/$public_receipt_name" --output public-repair-receipt.json
          cmp "$receipt" public-repair-receipt.json
          public_receipt_digest="sha256:$(sha256sum public-repair-receipt.json | awk '{print $1}')"
          jq -e --arg name "$public_receipt_name" --arg digest "$public_receipt_digest" '
            [.assets[] | select(.name == $name and .digest == $digest)] | length == 1
          ' public-release.json >/dev/null
          version="${tag#v}"
          standard_name="One-Person-Lab-${version}-mac-arm64.dmg"
          linux_name="One-Person-Lab-${version}-linux-x64.deb"
          for name in "$standard_name" "$linux_name" opl-app-component-manifest.json; do
            jq -e --arg name "$name" '[.assets[] | select(.name == $name)] | length == 1' public-release.json >/dev/null
          done
          {
            echo "tag=$tag"
            echo "app_sha=$app"
            echo "shell_sha=$shell"
            echo "framework_sha=$framework"
            echo "component_manifest_digest=$(jq -er '[.assets[] | select(.name == "opl-app-component-manifest.json")][0].digest' public-release.json)"
            echo "installer_name=opl-install.sh"
            echo "installer_digest=$(jq -er .replacement.next.digest "$receipt")"
            echo "previous_installer_digest=$(jq -er .replacement.previous.digest "$receipt")"
            echo "standard_artifact_name=$standard_name"
            echo "standard_artifact_digest=$(jq -er --arg name "$standard_name" '[.assets[] | select(.name == $name)][0].digest' public-release.json)"
            echo "linux_artifact_name=$linux_name"
            echo "linux_artifact_digest=$(jq -er --arg name "$linux_name" '[.assets[] | select(.name == $name)][0].digest' public-release.json)"
          } >> "$GITHUB_OUTPUT"

  certify-linux-x64:
    if: ${{ needs.resolve-release-set.outputs.applicable == 'true' }}
    needs: [resolve-release-set]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Download and install exact same-tag Linux Desktop
        env:
          RELEASE_TAG: ${{ needs.resolve-release-set.outputs.tag }}
          ARTIFACT_NAME: ${{ needs.resolve-release-set.outputs.linux_artifact_name }}
          ARTIFACT_DIGEST: ${{ needs.resolve-release-set.outputs.linux_artifact_digest }}
          INSTALLER_NAME: ${{ needs.resolve-release-set.outputs.installer_name }}
          INSTALLER_DIGEST: ${{ needs.resolve-release-set.outputs.installer_digest }}
        shell: bash
        run: |
          set -euo pipefail
          release_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}"
          curl --fail --location --retry 0 "$release_base/$ARTIFACT_NAME" --output "$ARTIFACT_NAME"
          curl --fail --location --retry 0 "$release_base/$INSTALLER_NAME" --output "$INSTALLER_NAME"
          test "sha256:$(sha256sum "$ARTIFACT_NAME" | awk '{print $1}')" = "$ARTIFACT_DIGEST"
          test "sha256:$(sha256sum "$INSTALLER_NAME" | awk '{print $1}')" = "$INSTALLER_DIGEST"
          package="$(dpkg-deb -f "$ARTIFACT_NAME" Package)"
          version="$(dpkg-deb -f "$ARTIFACT_NAME" Version)"
          architecture="$(dpkg-deb -f "$ARTIFACT_NAME" Architecture)"
          test "$architecture" = amd64
          if dpkg-query -W "$package" >/dev/null 2>&1; then
            echo '::error::Linux certification requires a clean runner without the Desktop package preinstalled.'
            exit 1
          fi
          bash "$INSTALLER_NAME" --desktop --release-tag "$RELEASE_TAG" --no-open
          test "$(dpkg-query -W -f='${Version}' "$package")" = "$version"
          test "$(dpkg-query -W -f='${Architecture}' "$package")" = "$architecture"
          jq -n --arg tag "$RELEASE_TAG" --arg name "$ARTIFACT_NAME" --arg digest "$ARTIFACT_DIGEST" \
            --arg package "$package" --arg version "$version" --arg architecture "$architecture" \
            '{schema:"opl_app_linux_same_tag_desktop_install.v1",status:"passed",release_tag:$tag,artifact:{name:$name,digest:$digest},installed_package:{name:$package,version:$version,architecture:$architecture},rebuilt:false,remaining:[]}' \
            > linux-x64-same-tag-install.json
      - name: Upload Linux Desktop certification evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: opl-linux-desktop-certification-${{ github.run_id }}
          path: linux-x64-same-tag-install.json
          if-no-files-found: error
          retention-days: 30

  admit-macos-vm:
    if: ${{ needs.resolve-release-set.outputs.applicable == 'true' && needs.resolve-release-set.outputs.certify_macos == 'true' }}
    needs: [resolve-release-set]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      eligible: ${{ steps.capability.outputs.eligible }}
      reason_code: ${{ steps.capability.outputs.reason_code }}
    steps:
      - name: Admit exact Tart capability for post-publication certification
        id: capability
        env:
          GH_TOKEN: ${{ secrets.OPL_RUNNER_INVENTORY_TOKEN || github.token }}
        shell: bash
        run: |
          set -euo pipefail
          if ! gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/runners?per_page=100" > runners.json; then
            echo 'eligible=false' >> "$GITHUB_OUTPUT"
            echo 'reason_code=operator_deferred' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          eligible="$(jq -r '
            [.[]?.runners[]? | select(
              .status == "online" and .busy == false
              and ([.labels[].name] | index("self-hosted") != null)
              and ([.labels[].name] | index("macOS") != null)
              and ([.labels[].name] | index("ARM64") != null)
              and ([.labels[].name] | index("opl-cert-mac-tart") != null)
            )] | length > 0
          ' runners.json)"
          if [ "$eligible" = true ]; then
            echo 'eligible=true' >> "$GITHUB_OUTPUT"
            echo 'reason_code=admitted' >> "$GITHUB_OUTPUT"
          else
            echo 'eligible=false' >> "$GITHUB_OUTPUT"
            echo 'reason_code=operator_deferred' >> "$GITHUB_OUTPUT"
          fi

  certify-standard-vm:
    if: ${{ needs.resolve-release-set.outputs.applicable == 'true' && needs.resolve-release-set.outputs.certify_macos == 'true' && needs.admit-macos-vm.outputs.eligible == 'true' }}
    needs: [resolve-release-set, admit-macos-vm]
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/opl-first-run-vm.yml
    with:
      release_tag: ${{ needs.resolve-release-set.outputs.tag }}
      published_artifact_name: ${{ needs.resolve-release-set.outputs.standard_artifact_name }}
      published_artifact_digest: ${{ needs.resolve-release-set.outputs.standard_artifact_digest }}
      artifact_app_ref: ${{ needs.resolve-release-set.outputs.app_sha }}
      shell_ref: ${{ needs.resolve-release-set.outputs.shell_sha }}
      smoke_harness_ref: ${{ needs.resolve-release-set.outputs.shell_sha }}
      framework_ref: ${{ needs.resolve-release-set.outputs.framework_sha }}
      package_profile: standard
      diagnostic_scope: post_publication_optional_certification
      require_macos_gatekeeper: true

  receipt:
    if: ${{ always() }}
    needs: [resolve-release-set, certify-linux-x64, admit-macos-vm, certify-standard-vm]
    runs-on: ubuntu-latest
    steps:
      - name: Write Desktop Release Set certification receipt
        shell: bash
        run: |
          jq -n --arg tag '${{ needs.resolve-release-set.outputs.tag }}' \
            --arg source '${{ needs.resolve-release-set.outputs.source_run_id }}' \
            --arg applicable '${{ needs.resolve-release-set.outputs.applicable }}' \
            --arg reason '${{ needs.resolve-release-set.outputs.reason_code }}' \
            --arg kind '${{ needs.resolve-release-set.outputs.certification_kind }}' \
            --arg old_installer '${{ needs.resolve-release-set.outputs.previous_installer_digest }}' \
            --arg new_installer '${{ needs.resolve-release-set.outputs.installer_digest }}' \
            --arg linux '${{ needs.certify-linux-x64.result }}' \
            --arg admission '${{ needs.admit-macos-vm.outputs.reason_code }}' \
            --arg standard '${{ needs.certify-standard-vm.result }}' \
            '{schema:"opl_app_desktop_release_set_certification.v1",status:(if $applicable != "true" then "not_applicable" elif $kind == "repair_additive" and $linux != "success" then "failed" else "complete" end),applicable:($applicable == "true"),reason_code:$reason,certification_kind:$kind,release_tag:$tag,source_run_id:$source,installer_digest_chain:(if $kind == "repair_additive" then {previous:$old_installer,current:$new_installer} else null end),linux_x64:$linux,macos_vm_admission:$admission,macos_standard:$standard,required_for_publication:false,remaining:(if $kind == "repair_additive" and $linux != "success" then ["linux_clean_install"] else [] end)}' \
            > desktop-release-set-certification.json
      - name: Upload Desktop Release Set certification receipt
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: opl-desktop-release-set-certification-${{ github.run_id }}
          path: desktop-release-set-certification.json
          if-no-files-found: error
          retention-days: 30
      - name: Require clean Linux certification for additive installer repair
        if: ${{ needs.resolve-release-set.outputs.certification_kind == 'repair_additive' }}
        shell: bash
        run: test '${{ needs.certify-linux-x64.result }}' = success
