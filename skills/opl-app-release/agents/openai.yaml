interface:
  display_name: "OPL App 发布"
  short_description: "按 Studio 发布矩阵执行、恢复和优化 OPL App 各渠道及附加发布"
  default_prompt: "使用 $opl-app-release 按当前发布矩阵与 SOP 推进已授权的 OPL App 发布或流程优化，复用有效检查点并核实全部要求渠道的公开终态。"
policy:
  allow_implicit_invocation: true
